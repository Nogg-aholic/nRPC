# serviceComposition

> **HTTP:** `POST /api/serviceComposition` | **Type:** `async function serviceComposition(): Promise<{ readonly mountRpcNamespace: <TTarget extends MutableRecord, TSurface>(target: TTarget, descriptor: RpcNamespaceDescriptor, surface: TSurface) => TTarget; }>` | **Location:** [`../../src/index.ts:182`](../../src/index.ts:182)

## Signature

```typescript
async function serviceComposition(): Promise<{ readonly mountRpcNamespace: <TTarget extends MutableRecord, TSurface>(target: TTarget, descriptor: RpcNamespaceDescriptor, surface: TSurface) => TTarget; }>
```

## Returns

`{ readonly mountRpcNamespace: <TTarget extends MutableRecord, TSurface>(target: TTarget, descriptor: RpcNamespaceDescriptor, surface: TSurface) => TTarget; }`

Return value

## Implementation

```typescript
{
		mountRpcNamespace,
	}
```

## Dependencies

### Internal

#### `{ readonly mountRpcNamespace: <TTarget extends MutableRecord, TSurface>(target: TTarget, descriptor: RpcNamespaceDescriptor, surface: TSurface) => TTarget; }` (type)

**Description:** Return type
